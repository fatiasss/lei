
CC = gcc
CFLAGS = -Wall

progs = fork1 fork2 fork2b child fork3 fork3b fork4 sig1 sig2 sig3 myls
objs = $(progs:%=%.o)

.PHONY:     all clean cleanall

all:		$(progs)

$(progs):	%:	%.o
	$(CC) -o $@ $<

$(objs):	%.o:	%.c
	$(CC) $(CFLAGS) -c -o $@ $<

clean:
	rm -f *~ *.o

cleanall:	clean
	rm -f $(progs) 
